import exp from "constants";

export enum EasingType {
    None = 1,
    sineOut = 2,
    quadOut = 3,
    backOut = 4,
    backIn = 5,
}

export enum ActionType {
    None = 1,
    MOVE = 2,
    SCALE = 3,
    OPACITY = 4,
}